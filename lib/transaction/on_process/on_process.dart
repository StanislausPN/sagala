import 'package:flutter/material.dart';

class OnProcess extends StatelessWidget {
  const OnProcess({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(child: Text('Dalam Proses')),
    );
  }
}
